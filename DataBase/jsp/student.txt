create table student
(
ID number not null,
NAME VARCHAR2(4000),
GRADE1 VARCHAR2(4000),
GRADE2 VARCHAR2(4000),
GRADE3 VARCHAR2(4000),
GRADE4 VARCHAR2(4000),
GRADE5 VARCHAR2(4000),
GRADE6 VARCHAR2(4000)
);

ALTER TABLE STUDENT ADD (
  CONSTRAINT stu_pk PRIMARY KEY (ID));
  
  CREATE SEQUENCE stu_seq START WITH 1001;
  
  CREATE OR REPLACE TRIGGER stu_bir 
  BEFORE INSERT ON student 
  FOR EACH ROW
  BEGIN
  SELECT stu_seq.NEXTVAL
  INTO   :new.id
  FROM   dual;
END;


INSERT INTO student (name, GRADE1, GRADE2, GRADE3, GRADE4, GRADE5,GRADE6)
   VALUES ('ARGHO', 'A+', 'A+', 'A-', 'A', 'B','B+');


ALTER TABLE student
  ADD password VARCHAR2(4000);
  
  ALTER TABLE student
  ADD email VARCHAR2(4000);